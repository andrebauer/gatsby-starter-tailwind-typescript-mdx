type BooksYaml implements Node {
  title: String!
  year: Int!
  authors: [Person!]!
}

type Person {
  firstname: String!
  lastname: String!
}

type Site implements Node {
  siteMetadata: SiteMetadata!
}

type SiteMetadata {
  title: String!
  description: String!
  author: String!
}
